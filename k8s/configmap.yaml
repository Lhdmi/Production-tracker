apiVersion: v1
kind: ConfigMap
metadata:
  name: production-tracker
  namespace: production-tracker
data:
  PORT: "3001"
  STORAGE_DRIVER: "local"
  # URL publique de l'app (doit être HTTPS pour la caméra / la PWA)
  PUBLIC_URL: "https://prodtrack.example.com"
